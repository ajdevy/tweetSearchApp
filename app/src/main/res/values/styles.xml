<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!--<style name="AppTheme" parent="AppBaseTheme">-->
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="actionMenuTextColor">@android:color/white</item>
        <item name="colorControlNormal">@color/edit_text_underline</item>
        <item name="colorControlActivated">@color/edit_text_underline</item>
        <item name="colorControlHighlight">@color/edit_text_underline</item>
    </style>

    <style name="MainTheme" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="LoginEditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="android:textColor">@color/edit_text_color</item>
        <item name="android:textColorHint">@color/edit_text_hint_color</item>
        <item name="colorControlNormal">@color/edit_text_underline</item>
        <item name="colorControlActivated">@color/edit_text_underline</item>
        <item name="colorControlHighlight">@color/edit_text_underline</item>
    </style>

    <style name="LoginButtonStyle" parent="ThemeOverlay.AppCompat.Dark">
        <item name="colorButtonNormal">@color/login_button_color</item>
        <item name="colorAccent">@color/login_button_color</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="TweetButtonStyle" parent="Base.Widget.AppCompat.Button.Borderless">
        <item name="android:textColor">@color/primary_green</item>
    </style>
</resources>